<% @title = 'Edit "' + h(@article.title) + '" Article' %>

<% content_for(:breadcrumb) do %>
    <li><%= link_to 'Articles', articles_path %></li>
    <li><%= link_to h(@article.title), article_path(@article) %></li>
    <li class="active">Edit</li>
<% end %>

<%= render :partial => 'form' %>
